import { Input } from "antd";
import React from "react";
import { FaSearch } from "react-icons/fa";

function TransactionListView ({ transactions}) {
	return (
    <div className={'h-auto px-4 py-4 flex items-center bg-primary-brand-darker overflow-hidden'}>
    </div>
	);
};

export default TransactionListView;
